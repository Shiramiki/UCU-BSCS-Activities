# Input the length of the base and height of the parallelogram
base = float(input("Enter the length of the base: "))
height = float(input("Enter the height of the parallelogram: "))

area = base * height

# Display the result
print(f"The area is: {area}")
